import {ExceptionHandler} from './exception-handler';

describe('ExceptionHandler', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ExceptionHandler()).toBeTruthy();
  });
});
