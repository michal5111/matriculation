export class Programme {
  id: string;
  name: string;
  usosId: string;

  constructor(id: string, name: string, usosId: string) {
    this.id = id;
    this.name = name;
    this.usosId = usosId;
  }
}
