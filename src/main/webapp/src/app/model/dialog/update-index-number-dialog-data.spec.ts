import { UpdateIndexNumberDialogData } from './update-index-number-dialog-data';

describe('UpdateIndexNumberDialogData', () => {
  it('should create an instance', () => {
    expect(new UpdateIndexNumberDialogData()).toBeTruthy();
  });
});
